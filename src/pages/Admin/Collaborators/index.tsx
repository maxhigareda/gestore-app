import React from 'react';

const CollaboratorsPage: React.FC = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1 style={{ fontSize: '2rem', marginBottom: '1rem' }}>Colaboradores</h1>
            <p>Gestión de colaboradores, creación, bajas y grupos.</p>
        </div>
    );
};

export default CollaboratorsPage;
