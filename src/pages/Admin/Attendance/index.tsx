import React from 'react';

const AttendancePage: React.FC = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <h1 style={{ fontSize: '2rem', marginBottom: '1rem' }}>Asistencia</h1>
            <p>Gestión de ausencias, calendario e incapacidades.</p>
        </div>
    );
};

export default AttendancePage;
